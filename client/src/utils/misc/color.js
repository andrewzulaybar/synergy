/**
 * Line chart colors.
 */
const fillColor = 'rgba(104, 145, 229, 0.3)';
const strokeColor = 'rgba(104, 145, 229, 1)';
const lineChart = {
  fillColor,
  strokeColor,
};

/**
 * Doughnut chart colors.
 */
const doughnutChart = {
  colors: [
    'rgba(98, 107, 206, 0.7)',
    'rgba(104, 145, 229, 0.7)',
    'rgba(129, 175, 244, 0.7)',
    'rgba(176, 207, 247, 0.7)',
    'rgba(210, 224, 241, 0.7)'
  ],
};

/**
 * Tooltip colors.
 */
const backgroundColor = 'rgba(0, 0, 0, 0.7)';
const textColor = 'rgba(255, 255, 255, 1)';
const tooltip = {
  backgroundColor,
  textColor,
};

export {
  doughnutChart,
  lineChart,
  tooltip,
};